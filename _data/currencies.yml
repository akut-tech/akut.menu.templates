# Maps the numeric `Currency` value in the menu JSON to a currency code and the
# symbol used when formatting prices.
#
# Mirrors the akut.domain `Currency` enum:
#   Euro = 1, Dollar = 2, Pound = 3
1: { code: EUR, symbol: "€" }
2: { code: USD, symbol: "$" }
3: { code: GBP, symbol: "£" }